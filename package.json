{
  "name": "fractalxr",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "description": "WebXR 3D fractal flame generator for the Meta Quest 3",
  "scripts": {
    "dev": "vite",
    "bake": "vite --config vite.bake.config.ts",
    "art": "vite --config vite.bake.config.ts --open /icon.html",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test:stereo": "vite --config vite.http.config.ts --open /stereo-test.html",
    "zoom": "vite --config vite.http.config.ts --open /zoom.html"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "three": "^0.184.0",
    "@sparkjsdev/spark": "^2.1.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "@types/three": "^0.184.1",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "wrangler": "^4.103.0"
  }
}
